import 'package:flutter/material.dart';

class TransformationItem extends StatelessWidget {
  const TransformationItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}